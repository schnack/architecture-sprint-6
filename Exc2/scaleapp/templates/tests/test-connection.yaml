apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scaleapp.fullname" . }}-test-connection"
  labels:
    {{- include "scaleapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scaleapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
